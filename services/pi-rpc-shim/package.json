{
  "name": "pi-rpc-shim",
  "version": "0.1.0",
  "description": "OpenAI-compatible HTTP shim that drives the Pi coding agent via its RPC client. Plug into LibreChat (or any OpenAI-compat chat UI) to get a web frontend for Pi.",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.74.0",
    "express": "^5.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
